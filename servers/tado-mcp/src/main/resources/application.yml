spring:
  application:
    name: tado-mcp
  ai:
    mcp:
      server:
        name: tado
        version: 1.0.0
        type: SYNC
      sse:
        enabled: true
        message-endpoint: /mcp/sse

server:
  port: 8080
