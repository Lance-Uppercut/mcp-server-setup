[] configure Todoist mcp server
[] create mcp server wrapper for this https://github.com/Vaskivskyi/asusrouter so we can configure the two ac68u routers
   [] end goal is to have neiter expose the wifi network
